#include <stdio.h>

/**
 * main - Program that prints its name
 * @argc: Number of arguments in program
 * @argv: String of arguments in program
 *
 * Return: zero
 */

int main(__attribute__((unused)) int argc, char *argv[])

{
	printf("%s\n", argv[0]);

	return (0);
}
